name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.25'

    - name: Build
      run: |
        go build -v -o roady ./cmd/roady
        go build -v -o roady-plugin-mock ./cmd/roady-plugin-mock

    - name: Test
      run: go test -v -coverprofile=coverage.out ./...

    - name: Security Scan (Verdict)
      run: |
        # In a real environment, we'd install verdict or use a container
        # For now, we simulate the scan if the binary is missing
        if command -v verdict &> /dev/null; then
          verdict scan .
        else
          echo "Verdict not found, skipping security scan in CI environment."
        fi
      continue-on-error: true

  release:
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
